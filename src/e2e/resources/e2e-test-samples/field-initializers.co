struct foo {
    int x = 2, y = x * 2

    constructor(int y) {
        this.y = y
    }
}

void main() {
    foo a = foo()
    assert(a.x == 2)
    assert(a.y == 4)

    foo b = foo(15)
    assert(a.x == 2)
    assert(b.y == 15)
}
