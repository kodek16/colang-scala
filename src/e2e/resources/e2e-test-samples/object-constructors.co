struct foo {
    int x

    constructor(int x) {
        this.x = x
    }

    constructor(int a, int b) {
        x = a * b
    }
}

void main() {
    foo a = foo(6)
    assert(a.x == 6)

    foo b = foo(2, 7)
    assert(b.x == 14)
}
